<odoo>

    <!--    Testing Server ACtion-->
    <record model="ir.actions.server" id="demo">
        <field name="name">Testing Server Action</field>
        <field name="model_id" ref="model_estate_property"/>
        <field name="state">code</field>
        <field name="code">
            print("Hello World")
        </field>
    </record>


    <!--    Contextual Meny action adding demo-->
    <record model="ir.actions.server" id="action_reset_state">
        <field name="name">Change_state_to_new</field>
        <field name="model_id" ref="model_estate_property"/>
        <!--        <field name="groups_id" eval="[(4, ref('estate_property'))]"/>-->
        <field name="binding_model_id" ref="model_estate_property"/>
        <field name="state">code</field>
        <field name="binding_view_types">list</field>
        <field name="code">
            for rec in records:
            rec.write({'state': 'new'})
        </field>
    </record>


    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">tree,form,kanban</field>

        <field name="context">{'search_default_available': True}</field>
        <!-- Above line let us configure 'available' search filter to be set as default-->
    </record>


    <record id="estate_property_tree" model="ir.ui.view">
        <field name="name">estate.property.tree</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <tree string="Channel"
                  decoration-success="state=='offer_received' or state=='offer_accepted'"
                  decoration-bf="state=='offer_accepted'"
                  decoration-muted="state=='sold'"

            >
                <field name="state" optional="hide"/>
                <field name="name"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area" string='Living Area (sqm)'/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" optional="hide" string="Available From"/>
                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'Color'}"/>

            </tree>
        </field>
    </record>


    <record id="estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="TEST">

                <header>
                    <button states="new" name="set_as_sold" type="object" string="SOLD"/>
                    <button states="offer_accepted" name="set_as_sold" type="object" string="SOLD"/>
                    <button states="offer_received" name="set_as_sold" type="object" string="SOLD"/>

                    <button name="set_as_canceled" type="object" string="Canceled"
                            attrs="{'invisible':[('state','in',['sold','canceled'])]}"
                    />

                    <button name="set_reset" type="object" string="Reset"/>
                    <button name="%(demo)d" type="action" string="Print Hello"/>
                     <button name="%(report_property_offer_xlsx)d" type="action" string="Print Xlsx"/>

                    <field name="state" widget="statusbar"
                           statusbar_visible="new,offer_received,offer_accepted,sold,canceled"/>
                </header>

                <sheet>
                    <group>
                        <!--                        <field name="state"/>-->
                        <field name="name"/>
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'Color'}"/>
                    </group>

                    <group>
                        <group>
                            <field name="property_type_id" options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="postcode"/>
                            <field name="date_availability" string="Available From"></field>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_offer" string='Best Offer'/>
                            <field name="selling_price"></field>
                        </group>
                    </group>

                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area" string='Living Area (sqm)'/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" string="Garden Area (sqm)" attrs="{
                                    'invisible': [('garden','=',False)]
                                }"/>
                                <field name="garden_orientation" attrs="{
                                    'invisible': [('garden','=',False)]
                                }"/>
                                <field name="active"/>
                                <!--                                <field name="state"/>-->
                                <field name="total_area" string="Total Area (sqm)"/>
                            </group>
                        </page>

                        <page string="Offers">
                            <group>
                                <field name="offer_ids" string="Offers"
                                       attrs="{'readonly':[('state','in',['offer_accepted','sold','canceled'])]}"

                                />

                            </group>
                        </page>

                        <page string="Other Info">
                            <group>
                                <field name="salesperson_id" string="Salesman"/>
                                <field name="buyer_id" string="Buyer"/>
                            </group>
                        </page>
                        <!--<page string="Hola">
                            <field name="description"/>
                        </page>-->
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>


    <record id="estate_property_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id" records_draggable="false">
                <field name="state"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <b>
                                <field name="name" string="Name"/>
                            </b>
                            <br/>
                            Expected Price:
                            <field name='expected_price'/>
                            <br/>

                            <div t-if="record.state.raw_value == 'offer_received'">
                                Best Offer:
                                <field name="best_offer"/>
                                <br/>
                            </div>


                            <div t-if="record.state.raw_value == 'offer_accepted'">
                                Selling Price:
                                <field name="selling_price"/>
                                <br/>
                            </div>

                            <field name="tag_ids"/>
                        </div>


                    </t>

                </templates>
            </kanban>

        </field>
    </record>


    <record id="estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Estate Property Search">
                <field name="name"/>
                <field name="bedrooms"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="living_area" filter_domain="[('living_area','>=',self)]"/>
                <field name="facades"/>

                <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}"/>
                <filter string="Available Homes" name="available"
                        domain="['|',('state','=','new'),('state','=','offer_received')]"/>

            </search>
        </field>
    </record>


</odoo>